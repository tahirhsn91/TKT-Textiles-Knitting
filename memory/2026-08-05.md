# 2026-08-05 — Daily Log

## Context
- User: Tahir (WhatsApp group, webchat session). Repo: TKT-Textiles-Knitting, dev env on this VPS (vmi3479497), `develop` branch. Docker stack: backend (Express/Drizzle, port 8080 internal) + frontend (React/Vite, host 3001) + postgres.
- Branching rule (AGENTS.md): never work on main/develop directly; branches + PRs against develop. **Exception granted this session:** user asked for several tasks with "no PR until I ask" — all those changes sit uncommitted on local develop working tree.

## Key decisions & work (morning → noon UTC)

### 1. ALLOWED_ORIGINS dev/prod separation (Option 1 chosen)
- `docker-compose.yml` backend env uses `${ALLOWED_ORIGINS:-...}` — already parameterized.
- Dev = `.env` (localhost:3000, 169.58.108.61:3001, tahir.servecounterstrike.com); Prod = `.env.prod` (localhost:1000, 169.58.108.61:1001, FRONTEND_PORT=1001). Both git-ignored.
- Prod command: `docker compose --env-file .env.prod up -d`. Dev: `docker compose up -d`.
- Documented in README (PR #43, merged).

### 2. Rebase workflow (established convention)
- User's rule: whenever a PR merges into develop, rebase develop onto main, resolve conflicts, force-push (with confirmation).
- Manual rebase done ~10:30 UTC: develop onto main (635cb9f), 4 dup commits auto-skipped, 6 replayed, force-pushed with --force-with-lease → 195000d.
- Automated via GitHub Action: `.github/workflows/rebase-develop.yml` (PR #51, open) — runs on push to develop, rebases onto main, force-pushes, aborts loudly on conflicts, no-op guard prevents loops.

### 3. Hot reload for dev (PR #44, #45, #46 — all merged)
- `docker-compose.dev.yml`: backend → target builder + `tsx watch` + source mount; frontend → target builder + Vite dev server (HMR) + `VITE_API_TARGET=http://backend:8080` proxy; ports `!override` needed (`!override` tag replaces merged ports).
- Fixes discovered: `pino-pretty` was referenced by backend logger in dev but never in package.json → added as devDependency (#45). Docker Compose merges `ports` lists across files → `!override` (#46).
- This machine runs the dev stack on port 3001; a separate prod stack `tkt-textiles-prod` runs on port 1001 (different project dir).

### 4. Daily Production screen changes (PR #49, merged)
- Production date defaults to yesterday (`yesterdayIso()`), `max` blocks today+.
- Totals strip: Day total + Month-to-date (backend `monthToDate` aggregate, submitted-only) beside date filter; redesigned as unified Card (user rejected first version — wanted unified strip with dividers, icons in muted tiles; "looks good now").
- Reconciled rows: yellow tint, Reconciled badge, Locked label; new **view-only mode** (eye button, readOnly dialog, disabled fields, Close only).
- Add dialog date prefills page's selected date (defaultDate prop).
- Native date picker in dialog (replaced custom DatePicker after user asked — user prefers native controls); native `<select>` for all dropdowns (asked; I advised against but user chose native).
- Operator dropdown filtered to Production department (matched by department name, not hardcoded id).
- Roll weight > 30 kg → row red in dialog (user iterated: red → dark red → redder → bg-red-300 / dark:bg-red-900/80). Same flag in entries table via backend `hasHeavyRoll` (max rollWeight > 30).
- Typecheck: pre-existing TS errors in reports/index.tsx + yarn-to-fabric.tsx fixed (as unknown as casts, Array.isArray guards) — PR #48 merged.

### 5. Yarn Receipt screen (NEW — uncommitted, no PR yet)
Requirements gathered one-by-one with options (user wanted options): header+lines (Option B), Qty = bags/bales, net weight typed directly (kg, 3 decimals), same party master, free editing (later superseded — see reconcile), type+count per line (**type later removed at user request**), no extras, full daily-production mirror, menu under Daily Operations.
- Backend: `yarn_receipt_header` (receipt_date, party_id, status, created_by) + `yarn_receipt_detail` (yarn_count_id, quantity int bags, net_weight numeric 12,3). Migration `0005_yarn_receipt.sql` (renamed from 0004 to avoid journal collision — 0004_reconcile was applied manually, not in drizzle journal; FKs/index applied manually after partial apply).
- Routes: GET /yarn-receipts?date= (summary + monthToDate), GET/POST/PUT/DELETE /:id, GET /yarn-receipts/unreconciled?date=&partyId= (flattened lines).
- Frontend: /yarn-receipts page (mirror of daily production incl. totals strip), add-receipt-dialog (native inputs, line table type→count dropdown removed later, whole-bag validation), hooks use-yarn-receipts.ts, menu under Daily Operations (desktop + mobile), App.tsx route.

### 6. Yarn Receipt ↔ Transaction integration (uncommitted)
- Transaction type "Yarn Receipt" (id 3, code Yarn_Receipt) exists in transaction_type_master.
- Form auto-fetches unreconciled receipts per date+party, fills detail lines (yarn count, qty, net wt), yellow banner, `reconcileReceiptIds` claimed on save with concurrency guard (409 + rollback), mirroring Fabric_Production flow.
- `yarn_receipt_header` gained reconciled/reconciled_transaction_id/reconciled_at columns + index.
- Yarn Receipt screen: reconciled receipts → yellow row, Reconciled badge, view-only eye, Locked label; PUT/DELETE return 409 for reconciled (backend guard); dialog readOnly mode.
- Edit button added to Yarn Receipt action column.

## PR status
- Merged: #43 (env docs), #44 (hot reload), #45 (pino-pretty), #46 (port override), #47 (analytics chart overflow — aspect-video forced 462px width; fix w-full aspect-auto), #48 (TS errors), #49 (daily production improvements).
- Open: #50 (Production deployment develop→main, user's), #51 (rebase workflow action).
- Uncommitted (user said no PR until asked): sidebar menu change (Daily Production under new "Daily Operations" group), yarn receipt screen + integration + reconciled locking.

## Environment notes
- Dev stack: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build` (hot reload, port 3001).
- Backend auto-restarts via tsx watch; frontend HMR.
- `scripts/deploy.sh` had a pre-existing uncommitted CORS-check change — not mine, left alone.
- gh CLI authed as tahirhsn91. Branch `fix/dev-compose-port-override` existed earlier; develop is current.
- User speaks in WhatsApp-style; keep replies concise, offer options when requirements are vague; user asked "grill me one-by-one with options" for requirements gathering — liked that format.

### 7. Yarn Receipt analytics (afternoon, uncommitted)
- New `GET /api/yarn-receipts/analytics?date=` → day lines (count/brand/party/qty/net) + monthSeries (per-day totals month-to-date).
- New Analytics tab on Yarn Receipts screen (Tabs: Entries/Analytics) with 6 charts: by count, by brand, by party (pie), month trend (line, kg+bags, gap-filled), Count×Brand stacked bar, avg kg/bag. Mirrors production analytics tab style (ChartContainer w-full aspect-auto).
- Note: transient Vite JSX error during multi-edit was resolved; final state tsc-clean + HTTP 200.
- Existing reconciled test receipt (header 5, "tahir") was kept; seed data cleaned up.

### 8. Daily Delivery screen (new, uncommitted — no PR yet)
- Full family mirror per grilled spec: single-line (Date, Party, Challan # (renamed from Reference, free text), SL text optional, GSM number optional, Qty rolls, Net Weight kg, Entered By); yesterday default, today selectable, tomorrow+ blocked; Day+MTD totals strip; Analytics tab (by party pie, rolls by party bar, by GSM bar, month trend line with gap-fill); edit/delete; reconciled = yellow + view-only when consumed into Fabric Delivery tx.
- Backend: daily_delivery table (migration 0006_daily_delivery.sql handwritten — drizzle-kit needed TTY due to journal drift), routes with reconciliationBlock 409 guard, suggestions endpoint (D-<n>), unreconciled endpoint, monthSeries in summary.
- Transactions: Fabric_Dispatch type now consumes reconcileDeliveryIds with 409 guard; form auto-fills lines + yellow banner.
- Verified live: CRUD, optional fields null, consume + lock + 409 double-booking, cleanup done. Typecheck clean both sides. Note: tsx watch still flaky on route changes — used docker compose restart backend.

### 9. Afternoon/evening session (14:20–18:25 UTC)
- **PRs merged:** #55 (yarn receipt doc number), #57 (dialog widths + dev CORS tooling — merged by Tahir), #59 (daily delivery), #61 (dev banner + NUM_DECIMALS + tabs strip), #64 (master data UX, merged), #66 (tab strip follow-up, open).
- **Daily Delivery** completed: yarn type added after party (column `yarn_type_id`); analytics grew to 6 charts (added by yarn type pie + avg kg/roll bar). Default date → today for yarn receipts + deliveries (production stays yesterday). All in PR #59.
- **Rounding:** all `toFixed()` → 2 decimals, unified via `frontend/src/lib/format.ts` `NUM_DECIMALS` constant (54 call sites, 13 files). Dashboard "Net weight produced" KPI shows full value (`7,659.80 kg`) not "7.7K".
- **Dev-environment banner** (PR #61): fixed red strip top, gated on `import.meta.env.DEV`, production never ships it. Later fix: content column needed `pt-7` too (banner overlapped buttons) — pushed to PR #66.
- **Master Data UX overhaul** (PR #64, all 8 items): per-tab search (shared MasterTable), sticky action column, URL hash tab persistence (`#machine`), delete explains FK-23503 "in use", inactive-row dimming (Operators only), inline numeric validation (red hint + save block), add-row scroll-into-view, touch targets h-10 mobile + description max-w. Tab strip follow-up (PR #66): full-width spread via `sm:flex-1`, `sm:px-2` edge padding, TabsTrigger hover + cursor-pointer (shared component → all tab screens).
- **Transactions native dropdowns** — converted list/monthly-salary/payroll to native `<select>` (incl. multi-select via `<select multiple>`), then **reverted at user request** (git checkout, only layout.tsx banner remained).
- **Master data tabs strip bug** — `sm:flex-wrap` wrapped 12 tabs into multi-row block; fixed to single scrollable row.
- **User instructions honored:** "no PR until I ask" periods; explicit force-push approval only; "commit but no PR"; PRs created on request. User likes one-question-at-a-time grilling with A/B/C options for requirements.
- **Quirks:** tsx watch intermittently misses route-file changes → `docker compose restart backend` reliably picks them up. Vite transient "JSX closing tag" errors mid-multi-edit are harmless (final state fine). `.env.prod`/prod stack separate (tkt-textiles-prod, port 1001); deploy.sh has DB-schema-vs-migrations check now.
- **Open PRs:** #66 (tab strip + banner offset). Local branch fix/master-data-tab-strip. Committed locally earlier: 831807d (banner) + fe45424 (rounding) are in main/develop history via PRs.
- **HMR explained to user** (they asked) — dev-only hot code swap, not used in production builds.
# 2026-08-05 — Daily Log

## Context
- User: Tahir (WhatsApp group, webchat session). Repo: TKT-Textiles-Knitting, dev env on this VPS (vmi3479497), `develop` branch. Docker stack: backend (Express/Drizzle, port 8080 internal) + frontend (React/Vite, host 3001) + postgres.
- Branching rule (AGENTS.md): never work on main/develop directly; branches + PRs against develop. **Exception granted this session:** user asked for several tasks with "no PR until I ask" — all those changes sit uncommitted on local develop working tree.

## Key decisions & work (morning → noon UTC)

### 1. ALLOWED_ORIGINS dev/prod separation (Option 1 chosen)
- `docker-compose.yml` backend env uses `${ALLOWED_ORIGINS:-...}` — already parameterized.
- Dev = `.env` (localhost:3000, 169.58.108.61:3001, tahir.servecounterstrike.com); Prod = `.env.prod` (localhost:1000, 169.58.108.61:1001, FRONTEND_PORT=1001). Both git-ignored.
- Prod command: `docker compose --env-file .env.prod up -d`. Dev: `docker compose up -d`.
- Documented in README (PR #43, merged).

### 2. Rebase workflow (established convention)
- User's rule: whenever a PR merges into main, rebase develop onto main, resolve conflicts, force-push (with confirmation).
- Manual rebase done ~10:30 UTC: develop onto main (635cb9f), 4 dup commits auto-skipped, 6 replayed, force-pushed with --force-with-lease → 195000d.
- Automated via GitHub Action: `.github/workflows/rebase-develop.yml` (PR #51, open) — runs on push to develop, rebases onto main, force-pushes, aborts loudly on conflicts, no-op guard prevents loops.

### 3. Hot reload for dev (PR #44, #45, #46 — all merged)
- `docker-compose.dev.yml`: backend → target builder + `tsx watch` + source mount; frontend → target builder + Vite dev server (HMR) + `VITE_API_TARGET=http://backend:8080` proxy; ports `!override` needed (`!override` tag replaces merged ports).
- Fixes discovered: `pino-pretty` was referenced by backend logger in dev but never in package.json → added as devDependency (#45). Docker Compose merges `ports` lists across files → `!override` (#46).
- This machine runs the dev stack on port 3001; a separate prod stack `tkt-textiles-prod` runs on port 1001 (different project dir).

### 4. Daily Production screen changes (PR #49, merged)
- Production date defaults to yesterday (`yesterdayIso()`), `max` blocks today+.
- Totals strip: Day total + Month-to-date (backend `monthToDate` aggregate, submitted-only) beside date filter; redesigned as unified Card (user rejected first version — wanted unified strip with dividers, icons in muted tiles; "looks good now").
- Reconciled rows: yellow tint, Reconciled badge, Locked label; new **view-only mode** (eye button, readOnly dialog, disabled fields, Close only).
- Add dialog date prefills page's selected date (defaultDate prop).
- Native date picker in dialog (replaced custom DatePicker after user asked — user prefers native controls); native `<select>` for all dropdowns (asked; I advised against but user chose native).
- Operator dropdown filtered to Production department (matched by department name, not hardcoded id).
- Roll weight > 30 kg → row red in dialog (user iterated: red → dark red → redder → bg-red-300 / dark:bg-red-900/80). Same flag in entries table via backend `hasHeavyRoll` (max rollWeight > 30).
- Typecheck: pre-existing TS errors in reports/index.tsx + yarn-to-fabric.tsx fixed (as unknown as casts, Array.isArray guards) — PR #48 merged.

### 5. Yarn Receipt screen (NEW — uncommitted, no PR yet)
Requirements gathered one-by-one with options (user wanted options): header+lines (Option B), Qty = bags/bales, net weight typed directly (kg, 3 decimals), same party master, free editing (later superseded — see reconcile), type+count per line (**type later removed at user request**), no extras, full daily-production mirror, menu under Daily Operations.
- Backend: `yarn_receipt_header` (receipt_date, party_id, status, created_by) + `yarn_receipt_detail` (yarn_count_id, quantity int bags, net_weight numeric 12,3). Migration `0005_yarn_receipt.sql` (renamed from 0004 to avoid journal collision — 0004_reconcile was applied manually, not in drizzle journal; FKs/index applied manually after partial apply).
- Routes: GET /yarn-receipts?date= (summary + monthToDate), GET/POST/PUT/DELETE /:id, GET /yarn-receipts/unreconciled?date=&partyId= (flattened lines).
- Frontend: /yarn-receipts page (mirror of daily production incl. totals strip), add-receipt-dialog (native inputs, line table type→count dropdown removed later, whole-bag validation), hooks use-yarn-receipts.ts, menu under Daily Operations (desktop + mobile), App.tsx route.

### 6. Yarn Receipt ↔ Transaction integration (uncommitted)
- Transaction type "Yarn Receipt" (id 3, code Yarn_Receipt) exists in transaction_type_master.
- Form auto-fetches unreconciled receipts per date+party, fills detail lines (yarn count, qty, net wt), yellow banner, `reconcileReceiptIds` claimed on save with concurrency guard (409 + rollback), mirroring Fabric_Production flow.
- `yarn_receipt_header` gained reconciled/reconciled_transaction_id/reconciled_at columns + index.
- Yarn Receipt screen: reconciled receipts → yellow row, Reconciled badge, view-only eye, Locked label; PUT/DELETE return 409 for reconciled (backend guard); dialog readOnly mode.
- Edit button added to Yarn Receipt action column.

## PR status
- Merged: #43 (env docs), #44 (hot reload), #45 (pino-pretty), #46 (port override), #47 (analytics chart overflow — aspect-video forced 462px width; fix w-full aspect-auto), #48 (TS errors), #49 (daily production improvements).
- Open: #50 (Production deployment develop→main, user's), #51 (rebase workflow action).
- Uncommitted (user said no PR until asked): sidebar menu change (Daily Production under new "Daily Operations" group), yarn receipt screen + integration + reconciled locking.

## Environment notes
- Dev stack: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up --build` (hot reload, port 3001).
- Backend auto-restarts via tsx watch; frontend HMR.
- `scripts/deploy.sh` had a pre-existing uncommitted CORS-check change — not mine, left alone.
- gh CLI authed as tahirhsn91. Branch `fix/dev-compose-port-override` existed earlier; develop is current.
- User speaks in WhatsApp-style; keep replies concise, offer options when requirements are vague; user asked "grill me one-by-one with options" for requirements gathering — liked that format.

### 7. Yarn Receipt analytics (afternoon, uncommitted)
- New `GET /api/yarn-receipts/analytics?date=` → day lines (count/brand/party/qty/net) + monthSeries (per-day totals month-to-date).
- New Analytics tab on Yarn Receipts screen (Tabs: Entries/Analytics) with 6 charts: by count, by brand, by party (pie), month trend (line, kg+bags, gap-filled), Count×Brand stacked bar, avg kg/bag. Mirrors production analytics tab style (ChartContainer w-full aspect-auto).
- Note: transient Vite JSX error during multi-edit was resolved; final state tsc-clean + HTTP 200.
- Existing reconciled test receipt (header 5, "tahir") was kept; seed data cleaned up.

### 8. Daily Delivery screen (new, uncommitted — no PR yet)
- Full family mirror per grilled spec: single-line (Date, Party, Challan # (renamed from Reference, free text), SL text optional, GSM number optional, Qty rolls, Net Weight kg, Entered By); yesterday default, today selectable, tomorrow+ blocked; Day+MTD totals strip; Analytics tab (by party pie, rolls by party bar, by GSM bar, month trend line with gap-fill); edit/delete; reconciled = yellow + view-only when consumed into Fabric Delivery tx.
- Backend: daily_delivery table (migration 0006_daily_delivery.sql handwritten — drizzle-kit needed TTY due to journal drift), routes with reconciliationBlock 409 guard, suggestions endpoint (D-<n>), unreconciled endpoint, monthSeries in summary.
- Transactions: Fabric_Dispatch type now consumes reconcileDeliveryIds with 409 guard; form auto-fills lines + yellow banner.
- Verified live: CRUD, optional fields null, consume + lock + 409 double-booking, cleanup done. Typecheck clean both sides. Note: tsx watch still flaky on route changes — used docker compose restart backend.

### 9. Afternoon/evening session (14:20–18:25 UTC)
- **PRs merged:** #55 (yarn receipt doc number), #57 (dialog widths + dev CORS tooling — merged by Tahir), #59 (daily delivery), #61 (dev banner + NUM_DECIMALS + tabs strip), #64 (master data UX, merged), #66 (tab strip follow-up, open).
- **Daily Delivery** completed: yarn type added after party (column `yarn_type_id`); analytics grew to 6 charts (added by yarn type pie + avg kg/roll bar). Default date → today for yarn receipts + deliveries (production stays yesterday). All in PR #59.
- **Rounding:** all `toFixed()` → 2 decimals, unified via `frontend/src/lib/format.ts` `NUM_DECIMALS` constant (54 call sites, 13 files). Dashboard "Net weight produced" KPI shows full value (`7,659.80 kg`) not "7.7K".
- **Dev-environment banner** (PR #61): fixed red strip top, gated on `import.meta.env.DEV`, production never ships it. Later fix: content column needed `pt-7` too (banner overlapped buttons) — pushed to PR #66.
- **Master Data UX overhaul** (PR #64, all 8 items): per-tab search (shared MasterTable), sticky action column, URL hash tab persistence (`#machine`), delete explains FK-23503 "in use", inactive-row dimming (Operators only), inline numeric validation (red hint + save block), add-row scroll-into-view, touch targets h-10 mobile + description max-w. Tab strip follow-up (PR #66): full-width spread via `sm:flex-1`, `sm:px-2` edge padding, TabsTrigger hover + cursor-pointer (shared component → all tab screens).
- **Transactions native dropdowns** — converted list/monthly-salary/payroll to native `<select>` (incl. multi-select via `<select multiple>`), then **reverted at user request** (git checkout, only layout.tsx banner remained).
- **Master data tabs strip bug** — `sm:flex-wrap` wrapped 12 tabs into multi-row block; fixed to single scrollable row.
- **User instructions honored:** "no PR until I ask" periods; explicit force-push approval only; "commit but no PR"; PRs created on request. User likes one-question-at-a-time grilling with A/B/C options for requirements.
- **Quirks:** tsx watch intermittently misses route-file changes → `docker compose restart backend` reliably picks them up. Vite transient "JSX closing tag" errors mid-multi-edit are harmless (final state fine). `.env.prod`/prod stack separate (tkt-textiles-prod, port 1001); deploy.sh has DB-schema-vs-migrations check now.
- **Open PRs:** #66 (tab strip + banner offset). Local branch fix/master-data-tab-strip. Committed locally earlier: 831807d (banner) + fe45424 (rounding) are in main/develop history via PRs.
- **HMR explained to user** (they asked) — dev-only hot code swap, not used in production builds.

### 10. Master Data UX round 2 — items 4–5 (18:25–18:35 UTC, pre-compaction)
- **Branch `feat/master-data-ux`** holds UNCOMMITTED items 1–3: per-tab search (shared `MasterTable`), sticky action column, URL hash tab persistence. Typecheck clean, page HTTP 200. NOT committed yet; no PR until user asks.
- **Item 4 (usage context in delete dialog)** investigation findings:
  - Backend `backend/src/routes/masters.ts` `/masters/party/:id` = plain `db.delete().returning()`, no usage pre-check; FK violation surfaces as Postgres code 23503.
  - Live test: deleting in-use party id 14 "Lucky Knits" → HTTP 500 `{"error":"Internal server error","code":"23503"}`.
  - Error handler: `backend/src/app.ts` lines ~114–135; frontend error parser `frontend/src/vendor/api-client-react/custom-fetch.ts` surfaces title/detail/message/error fields.
  - Frontend delete handler `frontend/src/components/master-table.tsx` ~line 214 → generic toast "Failed to delete. It may be in use."
  - Options weighed: (a) lightweight — surface the 23503 code/message in the delete toast; (b) heavier — backend usage-count endpoint across ~12 master tables. Lean: (a) first.
- **Item 5 (inline validation: red hint under field + block save until valid)**: not yet started.
- **Next steps:** finalize item 4 approach (likely toast surfacing of FK error), implement item 5, then commit items 1–3 (+4/5) on feat/master-data-ux and ask user about PR. Pending user decision on items 4–5 scope.

### 11. Operator → Employee full rename (19:00–19:25 UTC, PR #68 OPEN)
- **User asked:** rename master-data "Operator" tab to "Employee", full stack incl. DB table names + backend routes (chose Option B after A/B/C question).
- **DB:** migration `0007_rename_operator_to_employee.sql` (handwritten, applied manually to dev heliumdb): `machine_operator_master`→`employee_master`, `operator_advances`→`employee_advances`, `operator_salary_records`→`employee_salary_records`, `operator_salary_settings`→`employee_salary_settings`; columns `operator_id`→`employee_id` everywhere (incl. salary_detail, daily_production_header), `machine_operator_id`→`machine_employee_id` (transaction_detail), `operator_name`→`employee_name` (salary_detail); FK/unique/pkey constraint names renamed.
- **Backend:** schema → employeeMasterTable/employeeSalarySettingsTable/employeeSalaryRecordsTable/employeeAdvancesTable; salaryDetailTable.employeeId/employeeName. Routes: `/masters/employee`, `/employees/advances`, `/employees/payroll-summary`, `/lookups/employee-master`, `/dashboard/employee-output`. File renames: `routes/operators.ts`→`employees.ts`, generated `createEmployeeBody.ts` + `employeeLookupItem.ts`. All queries/joins/filters updated.
- **Frontend:** master tab `value="employee"` label Employees, URL hash `#employee`; `pages/operators`→`pages/employees`; generated hooks `useListEmployeeMaster*`; daily production, payroll/advances, reports (machineEmployeeName/Id, label "Employee"), dashboard `employee-output` widget, CSV import mapping `"employee": "employeeName"`.
- **Ops:** `deploy.sh` schema check now follows `ALTER TABLE ... RENAME TO` (drops renamed-away, adds renamed-to) so prod deploy validates new table names.
- **Verified:** backend + frontend `tsc --noEmit` clean; masters page 200; `/api/masters/employee`, `/api/lookups/employee-master`, `/api/employees/advances`, `/api/employees/payroll-summary`, `/api/dashboard/employee-output` all 200; old `/api/dashboard/operator-output` 404s.
- **Branch:** `feat/rename-operator-to-employee` (from develop). **PR #68 OPEN** against develop. 37 files changed, 628+/584-.

### 12. Follow-up: machine_employee_id → employee_id (20:12 UTC)
- User asked to simplify transaction_detail FK: `machine_employee_id` → `employee_id` (delta on top of PR #68, same branch).
- Migration 0007 edited to final form (`machine_operator_id` → `employee_id` + constraint `transaction_detail_employee_id_employee_master_id_fk`); dev DB delta applied manually (column + constraint rename, verified via information_schema/pg_constraint).
- Code: schema `employeeId`, routes (reports filter `employeeId`, dashboard/reports joins), generated api-zod + frontend api.schemas, reports pages (`employeeName`/`employeeId`, `employees` var), transaction form detail field `employeeId`.
- Verified: both tsc clean; masters page 200; `/api/reports/data?employeeId=8` 200; payload shows `"employeeName"`; zero `machineEmployee`/`machine_employee` refs left.
- Commit 46c5557 pushed → PR #68 now 2 commits, still OPEN vs develop.
