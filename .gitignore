node_modules/
dist/

# Environment files — never commit. Copy .env.example to .env and fill in real values.
.env
.env.*
!.env.example
*.local
.DS_Store
logs/

# Media files (audio, screenshots, large files)
audio/
audio-simple/
screenshots/
screenshots-real/
media/
demo_video/

# Database backups
database/_restore_*/
db-backups/
# Ad-hoc dumps kept in the working tree (dev-preRestore-*.sql, prod-dump-*.sql). Real dumps
# were found sitting here untracked, one `git add -A` away from a public repo.
database/backups/

# Graphify knowledge-graph output + ClawHub skill sync metadata — local only
.graphify/
graphify-out/
.clawhub/
uploads/

# Agent workspace files — these belong to the workspace root (~/project/tkt-textiles-prod),
# not to this repo. They were tracked once, which put the agent's session notes, its skill
# library and a staged database dump into a public repository. AGENTS.md is the only agent
# file that belongs in git: it is the rules file other agents read.
SOUL.md
IDENTITY.md
USER.md
TOOLS.md
HEARTBEAT.md
DREAMS.md
MEMORY.md
memory/
skills/
openclaw-workspace-state.json
design/identity-preview.html
